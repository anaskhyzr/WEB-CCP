{
  "name": "mini-ecommerce-module",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install-all": "npm install --workspaces",
    "start:backend": "npm --workspace backend start",
    "start:frontend": "npm --workspace frontend run dev",
    "init-db": "npm --workspace backend run init-db"
  }
}
